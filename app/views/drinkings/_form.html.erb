<%= form_with(model: drinking) do |form| %>
  <% if drinking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(drinking.errors.count, 'error') %> prohibited this drinking from being saved:</h2>

      <ul>
        <% drinking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: 'display: block' %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :starts_at, style: 'display: block' %>
    <%= form.datetime_field :starts_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
