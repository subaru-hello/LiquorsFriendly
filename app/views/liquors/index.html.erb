<h1>Liquors#index</h1>
<ul>
  <% @liquors.each do |liquor| %>
    <li>
      <%= cell(:liquors, liquor)%>
    </li>
    <li>
      <%= liquor.price %>
    </li>
  <% end %>
</ul>

<%= link_to "New Liquor", new_liquor_path %>
